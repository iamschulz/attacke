import { RetroKnights } from "./retro-knights/config" assert { type: "json" };
import { VectorSpaceships } from "./vector-spaceships/config" assert { type: "json" };

export const themes = {
	RetroKnights,
	VectorSpaceships,
};
